<!-- Currency -->
<b-form-group label="Currency" label-for="currency-pair-input">
  <b-form-tags
    id="currency-pair-input"
    v-model="priceBand.currencyPairIds"
    no-outer-focus
    size="sm"
  >
    <template v-slot="{ tags, inputAttrs, inputHandlers, removeTag }">
      <b-form-input
        v-bind="inputAttrs"
        v-on="inputHandlers"
        v-model="currencySearch"
        placeholder="Type to search currency..."
        list="currency-autocomplete"
        size="sm"
        @input="onCurrencyInput"
      />
      <datalist id="currency-autocomplete">
        <option
          v-for="option in filteredCurrencies"
          :key="option.value"
          :value="option.text"
        />
      </datalist>
      <div class="mt-1">
        <b-badge
          v-for="tag in tags"
          :key="tag"
          variant="dark"
          class="mr-1 mt-1"
        >
          {{ tag }}
          <span style="cursor:pointer" @click="removeTag(tag)"> ×</span>
        </b-badge>
      </div>
    </template>
  </b-form-tags>
</b-form-group>

<!-- Trading Group -->
<b-form-group label="Trading Group" label-for="trading-group-input">
  <b-form-tags
    id="trading-group-input"
    v-model="priceBand.tradingGroupIds"
    no-outer-focus
    size="sm"
  >
    <template v-slot="{ tags, inputAttrs, inputHandlers, removeTag }">
      <b-form-input
        v-bind="inputAttrs"
        v-on="inputHandlers"
        v-model="tradingGroupSearch"
        placeholder="Type to search trading group..."
        list="trading-autocomplete"
        size="sm"
      />
      <datalist id="trading-autocomplete">
        <option
          v-for="option in filteredTradingGroups"
          :key="option.value"
          :value="option.text"
        />
      </datalist>
      <div class="mt-1">
        <b-badge
          v-for="tag in tags"
          :key="tag"
          variant="dark"
          class="mr-1 mt-1"
        >
          {{ tag }}
          <span style="cursor:pointer" @click="removeTag(tag)"> ×</span>
        </b-badge>
      </div>
    </template>
  </b-form-tags>
</b-form-group>
